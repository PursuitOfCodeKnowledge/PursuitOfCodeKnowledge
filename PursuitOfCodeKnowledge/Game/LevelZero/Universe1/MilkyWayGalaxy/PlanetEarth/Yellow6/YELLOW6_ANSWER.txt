Чтобы выполнить задание:
Первое нужно посмотреть где ты находишься, используй команду pwd:
pwd
К примеру она тебе покажет путь /home/sergey/Desktop/PursuitOfCodeKnowledge.git/PursuitOfCodeKnowledge/Game/LevelZero/Universe1/MilkyWayGalaxy/PlanetEarth/Yellow6, получается ты сейчас находишься в Yellow6, 
тебе нужно вернуться назад используй команду cd ..:
cd ..
После этого набери команду ls или ls -a и посмотри что находится вокруг тебя какие файлы и директории:
ls
или
ls -a
Затем набери команду mkdir чтобы создать директорию Yellow (в том случае если ещё не создавал директорию Yellow):
mkdir Yellow
Затем перейди директорию в Yellow6:
cd Yellow6
Потом скопируй файл YELLOW6.txt в директорию Yellow:
cp YELLOW6.txt ../Yellow/
Затем проверь что скопировал файл YELLOW6.txt в директорию Yellow при помощи команды ls:
ls ../Yellow/YELLOW6.txt
После перенеси  файл файл YELLOW6_ANSWER.txt в директорию Yellow:
mv YELLOW6_ANSWER.txt ../Yellow/
Потом удали файл YELLOW6.txt при помощи команды rm:
rm YELLOW6.txt
или
rm -i YELLOW6.txt
или
rm -f YELLOW6.txt
После выйди из этой директории Yellow6:
cd ..
Затем удали директорию Yellow6:
rm -r Yellow6
После убедись что удалил директорию:
ls 
или
ls -a

Если исследовал уже директорию Yellow5, то заходим в своё окружение(смотри директорию Blue1):
conda activate envlearnpython
Затем заходим в свою директорию LearnPython:
cd LearnPython
Потом создай директорию AlgorithmsAndDataStructures(в том случае если ещё не создавал директорию AlgorithmsAndDataStructures):
mkdir AlgorithmsAndDataStructures
Затем перейди в эту директорию AlgorithmsAndDataStructures:
cd AlgorithmsAndDataStructures
Итак создай файл dfs_search_element_tree.py:
touch dfs_search_element_tree.py
Потом открой файл dfs_search_element_tree.py:
nano dfs_search_element_tree.py
Пишем в файле:
class Node:
	def __init__(self, value):
		self.value = value
		self.left = None
		self.right = None

def dfs_search(node, target):
	if node is None:
		return False
	if node.value == target:
		return True
	return dfs_search(node.left, target) or dfs_search(node.right, target)

def print_tree(node, level=0):
	if node is not None:
		print_tree(node.right, level + 1)
		print(' ' * 4 * level + '->', node.value)
		print_tree(node.left, level + 1)

if __name__ == "__main__":
	root = Node(30)
	root.left = Node(25)
	root.right = Node(35)
	root.left.left = Node(20)
	root.left.right = Node(30)
	root.left.right.left = Node(15)
	root.left.right.right = Node(27)
	root.left.left.left = Node(10)
	root.left.left.right = Node(17)
	print("Дерево: ")
	print_tree(root)

	print(dfs_search(root, 10))
	print(dfs_search(root, 11))

Нажимаем ctrl + o, затем enter и выходим из файла ctrl + x, после запускаем скрипт:
python3 dfs_search_element_tree.py

После выходим из директории AlgorithmsAndDataStructures:
cd ..
Дальше выйди из папки LearnPython:
cd ..

Если ты исследовал все цвета с числами, то добавить все директории с цветами в архив с паролем (Colours.7z):
7z a -p8 -mhe=on Colours.7z Black/* Blue/* Green/* Orange/* Purple/* Red/* White/* Yellow/*

Затем удалить директории с цветами:
rm -r Black
rm -r Blue
rm -r Green
rm -r Orange
rm -r Purple
rm -r Red
rm -r White
rm -r Yellow

После этого идём в директорию Countries:
cd Countries

Потом распаковываем архив Russia.7z(Москва впервые упоминается в летописи в 1147 году, когда князь Юрий Долгорукий встретился с князем Святославом Ольговичем. Этот год считается годом основания города):
7z x Russia.7z -p1147

Итак переходим в директорию Russia:
cd Russia

Затем читаем информацию о России:
cat InfoAboutRussia.txt

To complete the task:
First you need to see where you are, use the pwd command:
pwd
For example, she'll show you the way  /home/sergey/Desktop/PursuitOfCodeKnowledge.git/PursuitOfCodeKnowledge/Game/LevelZero/Universe1/MilkyWayGalaxy/PlanetEarth/Yellow6
 it turns out that you are currently in Yellow6,
you need to go back using the cd .. :
cd ..
After that, type the command ls or ls -a and see what is around you, which files and directories:
ls
or
ls -a
Then type the mkdir command to create
the Yellow directory(if you didn't create Yellow directory):
mkdir Yellow
Then go to the directory in Yellow6:
cd Yellow6
Then  copy the file YELLOW6.txt to the Yellow:
cp YELLOW6.txt ../Yellow/
Then check that you copied the file YELLOW6.txt to the Yellow directory using the command ls:
ls ../Yellow/YELLOW6.txt
Then transfer the file YELLOW6_ANSWER.txt to the Yellow:
mv YELLOW6_ANSWER.txt ../Yellow/
Then delete the file YELLOW6.txt using the rm command:
rm YELLOW6.txt
or
rm -i YELLOW6.txt
or
rm -f YELLOW6.txt
Then exit this directory Yellow6:
cd ..
Then delete the Yellow6 directory:
rm -r Yellow6
After that, make sure that you have deleted the directory:
ls
or
ls -a

If you have already explored the Yellow5 directory, then we go into our environment (see the Blue1 directory):
conda activate envlearnpython
Then we go to our LearnPython directory:
cd LearnPython
Then create the  AlgorithmsAndDataStructures directory(if you didn't create AlgorithmsAndDataStructures directory):
mkdir AlgorithmsAndDataStructures
Then go to this  AlgorithmsAndDataStructures directory:
cd AlgorithmsAndDataStructures
So create a file dfs_search_element_tree.py:
touch dfs_search_element_tree.py
Then open the file dfs_search_element_tree.py:
nano dfs_search_element_tree.py
Write in file:
class Node:
	def __init__(self, value):
		self.value = value
		self.left = None
		self.right = None

def dfs_search(node, target):
	if node is None:
		return False
	if node.value == target:
		return True
	return dfs_search(node.left, target) or dfs_search(node.right, target)

def print_tree(node, level=0):
	if node is not None:
		print_tree(node.right, level + 1)
		print(' ' * 4 * level + '->', node.value)
		print_tree(node.left, level + 1)

if __name__ == "__main__":
	root = Node(30)
	root.left = Node(25)
	root.right = Node(35)
	root.left.left = Node(20)
	root.left.right = Node(30)
	root.left.right.left = Node(15)
	root.left.right.right = Node(27)
	root.left.left.left = Node(10)
	root.left.left.right = Node(17)
	print("Дерево: ")
	print_tree(root)

	print(dfs_search(root, 10))
	print(dfs_search(root, 11))

Нажимаем ctrl + o, затем enter и выходим из файла ctrl + x, после запускаем скрипт:
python3 dfs_search_element_tree.py  
 
After that, we exit the AlgorithmsAndDataStructures:
cd ..
Next, exit the LearnPython:
cd ..

Если ты исследовал все цвета с числами, то добавить все директории с цветами в архив с паролем (Colours.7z):
7z a -p8 -mhe=on Colours.7z Black/* Blue/* Green/* Orange/* Purple/* Red/* White/* Yellow/*

Затем удалить директории с цветами:
rm -r Black
rm -r Blue
rm -r Green
rm -r Orange
rm -r Purple
rm -r Red
rm -r White
rm -r Yellow

После этого идём в директорию Countries:
cd Countries

Потом распаковываем архив Russia.7z(Москва впервые упоминается в летописи в 1147 году, когда князь Юрий Долгорукий встретился с князем Святославом Ольговичем. Этот год считается годом основания города):
7z x Russia.7z -p1147

Итак переходим в директорию Russia:
cd Russia

Затем читаем информацию о России:
cat InfoAboutRussia.txt
